# 2026-09-16

## CLI-autoupdate 规格链独立审计（CodeBuddy）

- 完成对 `SPEC-v0.2.2.md`（含争议链：Zoo review、Cherry reject）的独立审计，产出 `SPEC-v0.2.2-codebuddy-review.md`（同目录，277 行）。
- 关键裁决：
  - Zoo 报告对 v0.2.1 **不成立**：13/17 条带节引用全部失配（含行数 246 报称 vs 实测 365）；其描述的"配置文件/安装流程/签名验证/附录"等结构在 v0.1/v0.2/v0.2.1 全库 0 命中；系统性按"通用产品"提要求。实际依据对象无法确定（推断边界）。
  - Cherry reject 核心主张与逐条判定**成立**（8 行引用核验表逐行准确、5+2+10=17 统计无遗漏）；瑕疵 2 处低量级（"没有真正读"归因超范围；超时引用混合 v0.2.2 事实）。
- v0.2.2 独立发现（两方均未覆盖）：
  - F1 高：晋升凭证生命周期缺口（UPTODATE 跳过 verify → 首轮迁移无凭证/陈旧凭证重播/手动回退被覆盖；§13-2 seed 路径无实现）。
  - F2 中：标记不闭合（TARGET_PRERELEASE / LOCAL_AHEAD / NO_REMOTE 缺失；REMOTE_FAIL 后 remote 落盘语义与直连回退时序未定义）。
  - F3 中：RUNTIME_ERROR 处置三处不一致（§5.7 终止 vs §9 段A 继续）；PARSE_ERROR/STATE_MISSING 无 agent 分支。
  - F4 低中：汇报"数字取 sync.json"但 sync.json 无计数。
  - F5 低：mise REPAIR 动作未定义；healthy 未分型。
  - F6 观察：锁模型自洽（§14-3 已自披露），SOP 层补 LOCKED 空转处置。
- 机器实况（只读核验）：mise claude 2.1.273 / codex 0.154.0；opencode npm 1.18.31；`D:\AI\Programs\CLI` 已存在（`Codex\codex.exe` 298,169,136 B、`opencode\opencode.exe` 179,998,248 B，**无 claude**）；旧路径 `Claude-Code-CLI`、`opencode` 仍在。档案 md 为旧时点值（2.1.272/0.152.0/1.18.30），非 spec 缺陷。
- 未核实项：claude mise 注册表现况（需 Cherry MISE_* 环境）；Zoo 实际审计对象。
- 会话状态：v0.2.2 脚本实现尚未产出（工作区仅 md 文档）。
